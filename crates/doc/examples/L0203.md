---
title: Duplicate variable section
lint_rule: duplicate-var-section
---
The same variable section type (VAR, VAR_INPUT, etc.) appears more than once in a POU. Merge them into one.

```iecst
FUNCTION fn1 : INT
VAR
    x : INT;
END_VAR
VAR
    y : INT;
END_VAR
    fn1 := x + y;
END_FUNCTION
```
