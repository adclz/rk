---
title: Duplicate TASK name
---
Two TASKs in the same configuration have the same name.

```iecst
CONFIGURATION config1
    RESOURCE res1 ON CPU
        TASK task1(PRIORITY := 1);
        TASK task1(PRIORITY := 2);
    END_RESOURCE
END_CONFIGURATION
```
