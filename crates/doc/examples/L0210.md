---
title: Redundant NOT
lint_rule: redundant-not
---
A double negation (`NOT NOT x`) can be simplified to just `x`.

```iecst
FUNCTION test : BOOL
VAR
    flag : BOOL;
END_VAR
    test := NOT NOT flag;
END_FUNCTION
```
