---
title: INTERVAL after PRIORITY in TASK
---
In a TASK configuration, INTERVAL must be declared before PRIORITY.

```iecst
CONFIGURATION config1
    RESOURCE res1 ON CPU
        TASK task1(PRIORITY := 1, INTERVAL := T#20ms);
    END_RESOURCE
END_CONFIGURATION
```
