---
title: Malformed duration
---
A `TIME` or `LTIME` literal the compiler cannot read: a unit it does not know, a missing unit, or components out of order.
A duration is written `T#1d2h3m4s5ms`.

```iecst
FUNCTION_BLOCK fb1
VAR
    t: TIME := T#5x;
END_VAR
END_FUNCTION_BLOCK
```
