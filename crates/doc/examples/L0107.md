---
title: Identity operation
lint_rule: identity-operation
---
An operation with an identity value has no effect (adding 0, multiplying by 1, etc.).

```iecst
FUNCTION test : INT
VAR
    x : INT;
END_VAR
    x := x + 0;
    test := x * 1;
END_FUNCTION
```
