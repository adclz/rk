---
title: Negated comparison
lint_rule: negated-comparison
---
`NOT (x = y)` can be simplified to `x <> y`, and similar for other comparison operators.

```iecst
FUNCTION test : BOOL
VAR
    x : INT;
    y : INT;
END_VAR
    test := NOT (x = y);
END_FUNCTION
```
