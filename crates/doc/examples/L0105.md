---
title: Self-comparison
lint_rule: self-comparison
---
A variable is compared to itself. The result is always TRUE or always FALSE.

```iecst
FUNCTION test : INT
VAR
    x : INT;
END_VAR
    IF x = x THEN
        test := 1;
    END_IF;
END_FUNCTION
```
