---
title: SINGLE after PRIORITY in TASK
---
In a TASK configuration, SINGLE must be declared before PRIORITY.

```iecst
CONFIGURATION config1
    RESOURCE res1 ON CPU
        TASK task1(PRIORITY := 1, SINGLE := var1);
    END_RESOURCE
END_CONFIGURATION
```
