---
title: SINGLE after INTERVAL in TASK
---
In a TASK configuration, SINGLE must be declared before INTERVAL.

```iecst
CONFIGURATION config1
    RESOURCE res1 ON CPU
        TASK task1(INTERVAL := T#20ms, SINGLE := var1, PRIORITY := 1);
    END_RESOURCE
END_CONFIGURATION
```
