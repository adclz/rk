---
title: Constant FOR loop bounds
lint_rule: constant-loop-bounds
---
A FOR loop has equal start and end bounds, so the body executes exactly once.

```iecst
FUNCTION test : INT
VAR
    i : INT;
END_VAR
    FOR i := 5 TO 5 DO
        test := i;
    END_FOR;
END_FUNCTION
```
