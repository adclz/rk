---
title: Literal of the wrong kind
---
The literal is not the kind of value the type takes: a fraction where a whole number is expected, or a number where `TRUE` or `FALSE` is.

```iecst
FUNCTION_BLOCK fb1
VAR
    x: INT := 5.5;
END_VAR
END_FUNCTION_BLOCK
```
