---
title: Explicit default FOR step
lint_rule: default-for-step
---
`BY 1` is the default step - writing it out adds nothing.

```iecst
FUNCTION test : INT
VAR i : INT; END_VAR
    FOR i := 0 TO 5 BY 1 DO
        test := test + 1;
    END_FOR;
END_FUNCTION
```
