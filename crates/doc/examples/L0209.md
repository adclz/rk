---
title: Boolean comparison
lint_rule: bool-comparison
---
A comparison with a boolean literal can be simplified. Use the variable directly or negate it.

```iecst
FUNCTION test : INT
VAR
    flag : BOOL;
END_VAR
    IF flag = TRUE THEN
        test := 1;
    END_IF;
END_FUNCTION
```
