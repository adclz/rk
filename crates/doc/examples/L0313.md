---
title: Yoda condition
lint_rule: yoda-condition
---
A literal value appears on the left side of a comparison. Swap operands for conventional order.

```iecst
FUNCTION test : INT
VAR
    x : INT;
END_VAR
    IF 5 = x THEN
        test := 1;
    END_IF;
END_FUNCTION
```
