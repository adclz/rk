---
title: Unreachable code
lint_rule: dead-code
---
A statement appears after an unconditional control flow statement (RETURN, EXIT, CONTINUE) and can never be reached.

```iecst
FUNCTION test : INT
    test := 1;
    RETURN;
    test := 2;
END_FUNCTION
```
