---
title: Single-element array
lint_rule: single-element-array
---
An array dimension has equal lower and upper bounds, resulting in an array with only one element.

```iecst
FUNCTION test : INT
VAR
    arr : ARRAY[5..5] OF INT;
END_VAR
    test := arr[5];
END_FUNCTION
```
