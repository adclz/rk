[package]
name = "ast"
version = "0.1.0"
authors = { workspace = true }
license = { workspace = true }
edition = { workspace = true }
publish = false

[dependencies]
auto-lsp = { workspace = true }
tree-sitter-rk = { path = "../tree-sitter" }

[dev-dependencies]
insta = { version = "1.43.1", features = ["filters"] }
rstest = "0.25.0"

[build-dependencies]
auto-lsp-codegen = { workspace = true }
auto-lsp = { workspace = true }
tree-sitter-rk = { path = "../tree-sitter" }
